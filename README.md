# fire-detection-project
fire-detection-project

## Model Weights

The backend expects a PyTorch model file at `backend/models/model.pth`.
These weights are not tracked in the repository. You can either train your own
model using a fire image dataset or download a pre-trained version from the
project's release page. After obtaining the file, place it in
`backend/models/` before running the backend.
